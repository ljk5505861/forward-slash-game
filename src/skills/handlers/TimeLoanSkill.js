export default { cast(sys,cfg,data,level,ctx){ if(sys.passiveState.timeLoanActive) return; const s=sys.scene; sys.passiveState.timeLoanActive=true; const flash=s.add.rectangle(360,640,720,1280,0xff4b4b,0.16).setScrollFactor(0).setDepth(2800); s.tweens.add({targets:flash,alpha:0,duration:data.windowMs,onComplete:()=>flash.destroy()}); const others=s.playerData.skills.filter(x=>x.id!=='time_loan'); others.forEach(x=>sys.cooldowns.set(x.id,s.getGameplayTime())); const active={ nextAt:s.getGameplayTime()+120, endAt:s.getGameplayTime()+data.windowMs, round:0, extraCasts:data.extraCasts, tick(){ if(this.round>=this.extraCasts) return; others.forEach(x=>sys.forceCast(x.id,{fromTimeLoan:true})); this.round+=1; this.nextAt=s.getGameplayTime()+420; }, onEnd(){ if(this.ended) return; this.ended=true; sys.passiveState.timeLoanActive=false; flash.destroy(); } }; sys.active.push(active); }};
