import Phaser from 'phaser';
import { ring, line } from './common.js';
export default { cast(sys,cfg,data,level,ctx){ const s=sys.scene; let target=s.targeting.all().sort((a,b)=>(b.isBoss?3:b.isElite?2:1)-(a.isBoss?3:a.isElite?2:1)||b.hp-a.hp)[0]; if(!target)return; const sword=s.add.rectangle(target.x,target.y-210,26,150,cfg.color,0.85).setDepth(155); s.time.delayedCall(data.delayMs,()=>{ if(s.isGameplayPaused?.()) return; if(!s.targeting.valid(target)) target=s.targeting.all().sort((a,b)=>b.hp-a.hp)[0]; if(!target){ sword.destroy(); return; } sword.setPosition(target.x,target.y-140); line(s,target.x,target.y-230,target.x,target.y,cfg.color,10); sys.hit(target,sys.damageValue(data.damage,ctx),cfg,level); if(data.radius){ ring(s,target.x,target.y,data.radius,cfg.color); s.targeting.all().filter(e=>e!==target&&Phaser.Math.Distance.Between(e.x,e.y,target.x,target.y)<=data.radius).forEach(e=>sys.hit(e,data.damage*0.55,cfg,level)); } sword.destroy(); }); }};
