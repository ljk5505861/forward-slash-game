const Phaser = globalThis.Phaser || { Math:{ Distance:{ Between:(x1,y1,x2,y2)=>Math.hypot(x2-x1,y2-y1), }, Angle:{ Between:(x1,y1,x2,y2)=>Math.atan2(y2-y1,x2-x1) } } };
import { ring, line } from './common.js';

const pickTarget = (s) => s.targeting.all().sort((a,b)=>(b.isBoss?3:b.isElite?2:1)-(a.isBoss?3:a.isElite?2:1)||b.hp-a.hp)[0];

export default { cast(sys,cfg,data,level,ctx){ const s=sys.scene; let target=pickTarget(s); if(!target)return; const sword=s.add.rectangle(target.x,target.y-210,26,150,cfg.color,0.85).setDepth(155); const active={ skillId:cfg.id,cfg, nextAt:s.getGameplayTime()+data.delayMs, endAt:s.getGameplayTime()+data.delayMs+20, done:false, tick(){ if(this.done) return; this.done=true; if(!s.targeting.valid(target)) target=pickTarget(s); if(!target){ this.onEnd(); return; } sword.setPosition(target.x,target.y-140); line(s,target.x,target.y-230,target.x,target.y,cfg.color,10); sys.hit(target,sys.damageValue(data.damage,ctx),cfg,level,ctx,sys.baseDamageValue(data.damage,ctx)); if(data.radius){ ring(s,target.x,target.y,data.radius,cfg.color); s.targeting.all().filter(e=>e!==target&&Phaser.Math.Distance.Between(e.x,e.y,target.x,target.y)<=data.radius).forEach(e=>sys.hit(e,sys.damageValue(data.damage*0.55,ctx),cfg,level,ctx,sys.baseDamageValue(data.damage*0.55,ctx))); } if(data.secondBlade){ const next=pickTarget(s); if(next&&next!==target){ sys.hit(next,sys.damageValue(data.damage*0.65,ctx),cfg,level,ctx,sys.baseDamageValue(data.damage*0.65,ctx)); if(data.radius){ ring(s,next.x,next.y,data.radius,cfg.color); s.targeting.all().filter(e=>e!==next&&Phaser.Math.Distance.Between(e.x,e.y,next.x,next.y)<=data.radius).forEach(e=>sys.hit(e,sys.damageValue(data.damage*0.35,ctx),cfg,level,ctx,sys.baseDamageValue(data.damage*0.35,ctx))); } } } this.onEnd(); }, onEnd(){ if(this.ended) return; this.ended=true; sword.destroy(); } }; sys.active.push(active); }};
