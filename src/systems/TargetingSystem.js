export default class TargetingSystem { constructor(scene){ this.scene=scene; } valid(e){ return !!(e?.active && !e.isDefeated && e.hp > 0); } all(){ return this.scene.enemies.filter((e)=>this.valid(e)); } nearestAhead(range=Infinity){ const px=this.scene.player.x; return this.all().filter(e=>e.x >= px - 20 && e.x - px <= range).sort((a,b)=>a.x-b.x)[0] || null; } random(){ const a=this.all(); return a.length ? a[Math.floor(Math.random()*a.length)] : null; } aroundPlayer(radius){ return this.all().filter(e=>Math.abs(e.x-this.scene.player.x)<=radius); } }
