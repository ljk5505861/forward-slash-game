import { DESIGN_WIDTH, DESIGN_HEIGHT } from '../config/gameConfig.js';
export default class ResultPanel { constructor(scene){ this.scene=scene; this.nodes=[]; } show(won,onRestart){ this.hide(); const bg=this.scene.add.rectangle(DESIGN_WIDTH/2,DESIGN_HEIGHT/2,600,360,0x111111,0.94).setScrollFactor(0).setDepth(4000); const t=this.scene.add.text(DESIGN_WIDTH/2,DESIGN_HEIGHT/2-80,won?'胜利！':'失败',{fontFamily:'Arial',fontSize:'52px',color:'#fff'}).setOrigin(0.5).setScrollFactor(0).setDepth(4001); const b=this.scene.add.text(DESIGN_WIDTH/2,DESIGN_HEIGHT/2+60,'重新开始',{fontFamily:'Arial',fontSize:'34px',color:'#fff',backgroundColor:'#2d6cdf',padding:{left:28,right:28,top:16,bottom:16}}).setOrigin(0.5).setScrollFactor(0).setInteractive({useHandCursor:true}).setDepth(4001); b.on('pointerdown',onRestart); this.nodes.push(bg,t,b); } hide(){ this.nodes.forEach(n=>n.destroy()); this.nodes=[]; } }
