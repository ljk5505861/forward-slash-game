---
name: forward-slash-game-dev
description: 用于 forward-slash-game 项目的开发、Bug 修复、平衡调整、测试、版本更新和 PR。当任务涉及本游戏仓库代码时使用。
---

# forward-slash-game 开发流程

## 项目与规则来源

- 按 Phaser 4.1.0 + Vite、720×1280 竖屏和手机优先体验理解项目；依赖变化以实际 package.json 为准。
- 每次读取当前 AGENTS.md 及修改目录内适用的 AGENTS.md，不用聊天记忆代替现有实现。
- 在上级指令允许的范围内，以用户最新明确要求和当前 AGENTS.md 为准；历史方案、旧代码和旧文档不能覆盖最新确认。
- 让 AGENTS.md 保存权威玩法、具体设计和长期约束；让本 Skill 只保存开发流程与方法。不复制 AGENTS.md 全文，不在这里固化当前版本、波数、商品池或技能数值。
- 用户主要使用手机。默认由当前具备仓库权限的助手执行已授权任务，不要求用户操作电脑；无权限时说明阻塞，可提供完整的云端 Codex 指令，不声称能够控制其他 Codex 会话。

## 每次任务的执行顺序

1. 核对工作区、当前分支、最新 main 的 SHA、package.json、src/config/version.js 和相关目录结构；无法核实远端时明确说明。保护用户未提交修改。
2. 阅读需求直接涉及的实现和测试，确认调用链、数据来源及实际失败路径。先理解再修改，不凭猜测重写。
3. 确定最小修改范围和验收点，简述本次改什么、不改什么。需求明确的小修改直接执行，不反复询问；只有规则不明、实质扩大范围或遇到权限阻塞时才询问。
4. 实施最小完整改动。一次只处理当前任务，优先不超过三个生产文件；确需超过时先解释。修 Bug 优先修根因，不用大范围调参数掩盖问题。
5. 执行下面的云端检查，审查实际 diff、边界行为和模式隔离。功能或修复按仓库约定同步版本及必要的版本断言；纯文档或 Skill 变动不擅自升级游戏版本。
6. 如实汇总修改、测试和未完成项，完成适用的提交与 PR 工作。不把创建 PR 当成验收完成。
7. 需要用户手机测试时，给出简短、具体的操作步骤及预期结果，不要求用户自行推导测试方法。

## 测试与手机验收

- 代码改动执行 `npm run build`，不是 `npm build`；从 package.json 和 scripts/ 中选择已有的相关自动测试及静态检查，不编造不存在的命令。
- 优先运行改动模块、相关回归和云端可快速完成的验证；检查 `git diff --check`。修 Bug 尽量增加复现真实失败路径的轻量行为测试，而非只匹配源码文字。
- 禁止主动执行、重跑或深入排查与当前任务无关的检查。每项验证都应能对应本次修改的文件、行为或实际依赖影响；不能因为仓库有现成脚本、全套 CI 包含该项，就认定本次必须执行。相关依赖回归可以检查，但不得扩大成无关系统的全面验收。
- 仅修改文档或 Skill 时，只检查 YAML frontmatter（如适用）、文件路径、内容一致性和 diff；不启动游戏构建、浏览器、战斗命中率、商店或整局游玩测试，也不要求用户为文档变动测试游戏。
- Skill 不能控制 GitHub Actions 的触发条件；工作流应按 PR 改动范围运行。仅改 Skill、文档或验证工作流时运行轻量检查并跳过游戏构建、回归与浏览器；只要涉及游戏文件，仍执行原有游戏验证。
- 区分仓库自动触发的 CI 与本任务主动选择的验证。不主动运行与任务无关的检查；但 PR 已触发的检查必须全部结束并通过，才能判断“可以合并”。自动 CI 即使因无关问题失败，也须如实报告并保持 Draft；不擅自重跑、扩大本任务修复范围，或修改工作流、AGENTS.md 和有效测试断言来绕过失败。
- 手机触控、真机游戏、实际战斗手感、需人工观察的视觉效果及长时间重复游玩，在云端不便、无法完成或明显耗时过长时，直接列为用户验收项。不要反复安装浏览器、重跑大量对局或搭建大型测试来证明简单修改。
- 按用户最新授权交接耗时的手机体验测试；交接不等于通过。遵守仍适用的 AGENTS.md 验证要求，不私自删除检查、放宽有效断言或绕过合并门禁。自动检查尚未完成或有失败时，保持 Draft 并说明原因。
- 区分本次通过、本次新增失败、基线已有失败、环境受限和未执行；已有失败必须有基线证据，不凭记忆认定。不得把少量样本或自动化通过说成整局平衡已完成。
- 给用户至多几步手机验收：从哪里进入 → 做什么 → 观察什么 → 正常结果是什么。需要反馈时说明截图、短录屏或现象即可，不要求桌面日志。

## 游戏修改边界

- 保留用户已经确认的核心循环、UI、数值体系和流程；商店、技能、战斗具体规则查最新 AGENTS.md、当前任务及代码，不套用历史方案。
- 默认仅调整普通模式平衡，保留测试模式规则；共用 Bug 修复可覆盖两种模式，但不得夹带数值调整。
- 针对具体问题机制调整少数变量；不直接全局增加怪物生命，不随意削弱普攻或整体属性。明确预期收益和副作用，避免同时改多个体系导致无法判断原因。
- 让构筑明显影响战斗表现，不做固定唯一配方；保留合理随机混搭通关的可能。技能独立获得、独立有效，不强制特定前置组合。
- 涉及战斗回调时检查目标同步死亡或销毁；涉及技能移除、场景关闭和重启时检查事件、计时器、特效与引用清理。保留失败施法不无故消耗冷却等现有安全约束。
- 不顺手重构、改名或修复无关问题。发现其他问题可报告，不未经允许一起修改。

## Git 与 PR

- 新任务从最新 main 建立独立分支。原 PR 尚未合并时，同一任务的后续修复继续原分支与原 PR；已合并功能的新修改使用新分支、新 PR。
- 只提交本任务文件，保护其他工作；不擅自覆盖、重置或清理用户修改。游戏功能 PR 由用户最终合并；用户明确要求新增或更新的纯 Skill PR，可由助手完成适用检查和审查后直接合并。
- 提出“可以合并”前，完成适用的本地验证，审查实际 diff、base/head 和边界行为，确认 review threads 已处理，并核实 PR 上全部自动检查均已结束且通过。游戏改动还须完成 AGENTS.md 要求的验证；用户手机体验未完成时，明确列为后续验收，不冒充已通过。
- 任何检查失败、尚在运行、未执行必要验证或审查未完成，都不得说“可以合并”或把 PR 标为 Ready；保持 Draft 并明确阻塞原因。即使失败与本任务无关，也先交付 Draft 和检查结果，不能凭 GitHub 显示可点合并或推测基线问题跳过全部检查。不要为等待检查延迟告知进度。

## 完成后的汇报格式

【版本/分支】实际版本、分支及必要的 SHA；版本未变则注明。

【修改内容】改动文件、行为变化及原因。

【明确没有修改的内容】相关玩法、模式、数值或流程边界。

【测试结果】实际执行的命令与结果；失败、未执行和限制分别说明。

【需要用户手机验证的内容】简短操作、观察点与预期；无需则明确写无。

【提交/PR】实际提交、PR 链接及状态；未创建则说明。游戏功能 PR 不自动合并。
